import 'package:flutter/material.dart';
import 'package:hooks_riverpod/hooks_riverpod.dart';

class InstantSwapDeliver extends ConsumerWidget {
  const InstantSwapDeliver({super.key});

  @override
  Widget build(BuildContext context, WidgetRef ref) {
    return SizedBox();
    //  SwapSideAmount(text: 'Deliver'.tr());
  }
}
