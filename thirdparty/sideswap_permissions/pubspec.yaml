name: sideswap_permissions
description: Sideswap permissions plugin.
publish_to: "none"
version: 0.0.5

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^11.3.0
  plugin_platform_interface: ^2.1.8
  sideswap_logger:
    git:
      url: https://github.com/sideswap-io/sideswap_logger.git
      ref: master

dev_dependencies:
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter
